const calculator = require('./calculator');

test('A soma da calculadora deve estar ok', () => {
  expect(calculator.sum(100,500)).toBe(600);
});

test('Somente numeros são permitidos', () => {
  numberLeft: expect.any(Number)
  numberRight: expect.any(Number)
});
  

